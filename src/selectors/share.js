export const canvasSelector = ({canvas}) => canvas;
export const cropBoxSelector = ({cropBox}) => cropBox;
export const imageSelector = ({image}) => image;
export const containerSelector = ({container}) => container;
export const optionsSelector = ({options}) => options;
